<div class="card">
    <div class="card-header">
        <div>
    <h2> <a class="link" href="/dashboard/edit/{{id}}">{{header}}</a></h2>
        <span>Posted by {{header}} on{{format_date date_created}} </span>
        </div>
    </div>
        <p class="card-body">{{description}}</p>
</div>

